fastapi==0.110.0
uvicorn==0.27.1
# ML framework (handled in Dockerfile for CPU/GPU hybrid support)
# torch==2.2.1
# torchvision==0.17.1
# segment-anything-2 (Installed via ultralytics for SAM 2)
pystac-client==0.7.6
stackstac==0.5.1
odc-stac==0.3.9
ultralytics
numpy<2.0
rasterio==1.3.9
geopandas==0.13.2
pydantic-settings==2.2.1
python-multipart==0.0.9
huggingface_hub
timelines
matplotlib
aiocache==0.12.0
# Note: geoai might be a custom library or specific repo, 
# assuming 'geoai' refers to standard tools or specific repo installation
# If it's a specific package like 'geowombat' or similar, we'd add it here.
